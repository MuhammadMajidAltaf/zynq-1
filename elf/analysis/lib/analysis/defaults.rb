module Defaults
  # Important magic constants (but not important enough to pass in every time...)
  NUM_TAKEN = 10
  NUM_PARALLEL = 3
  MIN_RUNTIME_PERCENTAGE = 5
  MIN_NUM_ARITH = 4
  MIN_LENGTH = 10
end
